{
	"name": "hina",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "run-p dev:*",
		"dev:wrangler": "wrangler pages dev dist --live-reload --port 3000",
		"dev:esbuild": "esbuild --bundle src/server.ts --format=esm --watch --outfile=dist/_worker.js",
		"style": "tailwindcss -i ./builder.css -o ./dist/pub/styles.css --watch --minify",
		"build": "esbuild --bundle src/server.ts --format=esm --outfile=dist/_worker.js",
		"deploy": "wrangler pages publish dist"
	},
	"dependencies": {
		"@planetscale/database": "*",
		"jose": "*",
		"nanoid": "^3.3.6"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "*",
		"daisyui": "^3.5.1",
		"esbuild": "*",
		"npm-run-all": "*",
		"tailwindcss": "^3.3.3",
		"wrangler": "*"
	}
}
